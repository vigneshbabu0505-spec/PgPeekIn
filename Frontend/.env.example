# Copy this file to `.env` and fill in the real value.
# Only NON-secret, browser-exposed configuration belongs here.
# Never put Supabase keys, DB passwords, or JWT secrets in VITE_ variables.

VITE_API_BASE_URL=http://localhost:8080
